using System.Diagnostics.CodeAnalysis;

namespace Integrate.EmailVerification.Models.Enum
{
    public enum EmailValidationStatus
    {
        Valid = 0,
        Invalid = 1,
        Unknown = 2
    }
}
